/**
 * Auto-updater stub — placeholder for electron-updater integration.
 * When ready, install `electron-updater` and replace this with the real setup.
 */
export function setupAutoUpdater(): void {
  console.log("auto-updater: not configured");
}
